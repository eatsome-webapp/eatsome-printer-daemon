# Sentry Crash Reporting (Rust Backend)
# Get your DSN from https://sentry.io/settings/projects/
# Format: https://<key>@<organization>.ingest.sentry.io/<project-id>
SENTRY_DSN=

# Environment (development, staging, production)
SENTRY_ENVIRONMENT=development

# Release version (automatically set from package.json in production)
SENTRY_RELEASE=

# Sample rate for performance monitoring (0.0 to 1.0)
# 0.1 = 10% of transactions are sent to Sentry
SENTRY_TRACES_SAMPLE_RATE=0.1

# Sentry Crash Reporting (React Frontend)
# Note: Vite requires VITE_ prefix for env vars to be exposed to client
VITE_SENTRY_DSN=
VITE_SENTRY_ENVIRONMENT=development
VITE_SENTRY_TRACES_SAMPLE_RATE=0.1
VITE_APP_VERSION=1.0.0
